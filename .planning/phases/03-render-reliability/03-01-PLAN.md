---
phase: 03-render-reliability
plan: 03-01
type: standard
autonomous: true
wave: 1
depends_on: []
requirements: [REND-01, REND-02]
---

# Plan 03-01: Render Test Infrastructure and Shadow Quality Policy

## Objective

Establish render-path test infrastructure and document shadow quality policy:
- Add at least one `@Tag("render")` test or document technical blocker (REND-01)
- Document measurable shadow quality behavior per RenderQuality tier (REND-02)

## Context

Phase 2 completed configuration and script lifecycle hardening. Phase 3 begins render reliability work before expanding GLB, shadow, star, and mesh rendering.

## Current State

**REND-01 (Render tests):**
- Maven render-test infrastructure configured (Surefire excludes `render` group; Failsafe runs `render` tagged tests)
- No `@Tag("render")` tests currently exist in `src/test/java`
- Evidence: Phase 1 baseline confirmed "no `@Tag("render")` tests were found under `src/test/java`"

**REND-02 (Shadow quality):**
- `RenderQuality.java` already defines `maxOccluders()` per tier: LOW=2, MEDIUM=4, HIGH=8
- `EclipseShadowManager.java` already uses `RenderQuality.maxOccluders()` to limit casters
- Missing: Explicit documentation of the quality policy

## Tasks

### Task 1: Investigate render test feasibility
**Type:** auto
**Verification:** Run `mvn -Dgroups=render verify` or inspect existing tests for JME context usage

**Investigation steps:**
1. Check if existing tests use JME context (e.g., `KepplrAppTrailDeclutterTest` uses TestHarness only, no JME)
2. Check if Failsafe configuration in pom.xml allows render tests to run
3. Determine if headless/software GL is feasible for a smoke test, or document blocker

**Outcome:** Either add a simple `@Tag("render")` smoke test, or document technical blocker in REND-01 verification.

### Task 2: Document shadow quality policy
**Type:** auto
**Verification:** Verify documentation exists in code or markdown

**Documentation should specify:**
1. Max occluders per receiver per RenderQuality tier (already implemented: 2/4/8)
2. Shadow model per tier: LOW = point-source binary, MEDIUM/HIGH = extended-source penumbra
3. Angular sort priority: largest angular radius (casterRadius²/dist²) wins — ensures Titan not evicted by smaller inner moons
4. Reference to ShadowGeometry math for penumbra computation

**Where to document:**
- Option A: JavaDoc on `RenderQuality.maxOccluders()` — already has partial docs
- Option B: New section in `doc/configuration.rst` or `doc/usersguide.rst`
- Option C: Both

## Success Criteria

1. Either: At least one `@Tag("render")` test exists and `mvn -Dgroups=verify` runs it, OR: Technical blocker documented with evidence
2. Shadow quality policy is explicitly documented with measurable values per RenderQuality tier
3. `mvn test` passes with no new failures

## Output Specification

- Modified/added test files: either new render test or documented blocker
- Documentation: shadow quality policy in code/docs
- Summary: 03-01-SUMMARY.md with REND-01 and REND-02 status
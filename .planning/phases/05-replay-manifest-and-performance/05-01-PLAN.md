# Plan 05-01: Replay, Manifest, and Performance

## Objective

Implement REPRO-01 (render manifest), REPRO-02 (deterministic documentation), and REPRO-03 (telemetry).

## Background

Phase 4 (Data Tooling) completed. Phase 5 adds manifest, deterministic documentation, and lightweight telemetry.

## Current State

| Requirement | Current State | Gap |
|-------------|-------------|-----|
| REPRO-01 | capture_info.json has basic fields | Missing appVersion, platform, config/kernel identity |
| REPRO-02 | No documented tolerances | Documentation needed |
| REPRO-03 | No frame time telemetry | Implementation needed |

## Tasks

### Task 1: Extend manifest fields (REPRO-01)
**Type:** auto
**Verification:** New fields in capture_info.json

Add fields to capture_info.json:
1. appVersion - from AppVersion.getVersion()
2. platform - os + arch + Java version
3. configIdentity - from KEPPLRConfiguration if available
4. kernelIdentity - from metakernel path
5. renderQuality - current tier from RenderQuality.current()

Add tests in CaptureServiceTest.java.

### Task 2: Document deterministic tolerances (REPRO-02)
**Type:** auto
**Verification:** Documentation exists

Create documentation for:
1. Camera pose tolerances (degrees for rotation, km for position)
2. ET progression tolerance (seconds)
3. Capture timing variance per platform
4. Platform-specific limits

Add to doc/scripting_examples.rst or new section.

### Task 3: Add frame time telemetry (REPRO-03)
**Type:** auto
**Verification:** Frame time recorded

Where to add:
1. In KepplrApp.simpleUpdate() - track frame duration
2. Add get/setFrameTimeMs() to SimulationState
3. Key scene counts (bodies, stars, trails)

Add non-render tests in CaptureServiceTest or new test.

## Success Criteria

1. capture_info.json includes version, platform, config, kernel, render quality
2. Deterministic tolerances documented
3. Frame time recorded in state (non-render)
4. mvn test passes

## Output

- Modified: CaptureService.java, AppVersion.java
- Tests: CaptureServiceTest.java
- Documentation: doc/scripting_examples.rst